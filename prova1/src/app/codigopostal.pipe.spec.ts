import { CodigopostalPipe } from './codigopostal.pipe';

describe('CodigopostalPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigopostalPipe();
    expect(pipe).toBeTruthy();
  });
});
